import {render} from "@testing-library/react";
import EventsTimelineRootPage from "../EventsTimelineRootPage";

test("Render EventsTimelineRootPage", () => {
  render(<EventsTimelineRootPage />);
});
